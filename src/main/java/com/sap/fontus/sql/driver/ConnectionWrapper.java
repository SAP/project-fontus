package com.sap.fontus.sql.driver;

import com.sap.fontus.sql.tainter.StatementTainter;
import com.sap.fontus.sql.tainter.Taint;
import net.sf.jsqlparser.JSQLParserException;
import net.sf.jsqlparser.parser.CCJSqlParserUtil;
import net.sf.jsqlparser.statement.Statements;

import java.sql.*;
import java.util.ArrayList;
import java.util.List;
import java.util.Map;
import java.util.Properties;
import java.util.concurrent.Executor;


public class ConnectionWrapper extends AbstractWrapper implements Connection{

    private final Connection delegate;
    //private static Logger logger = LoggerFactory.getLogger("ias.taintdriver");

    public static ConnectionWrapper wrap(Connection delegate) {
        if (delegate == null) {
            return null;
        }
        final ConnectionWrapper connectionWrapper = new ConnectionWrapper(delegate);
        return connectionWrapper;
    }

    protected ConnectionWrapper(Connection delegate) {
        super(delegate);
        if (delegate == null) {
            throw new NullPointerException("Delegate must not be null");
        }
        this.delegate = delegate;
    }

    public Connection getDelegate() {
        return delegate;
    }

    @Override
    public Statement createStatement() throws SQLException {
        return StatementWrapper.wrap(delegate.createStatement());
    }

    @Override
    public Statement createStatement(int resultSetType, int resultSetConcurrency) throws SQLException {
        return StatementWrapper.wrap(delegate.createStatement(resultSetType, resultSetConcurrency));
    }

    @Override
    public Statement createStatement(int resultSetType, int resultSetConcurrency, int resultSetHoldability) throws SQLException {
        return StatementWrapper.wrap(delegate.createStatement(resultSetType, resultSetConcurrency, resultSetHoldability));
    }

    @Override
    public PreparedStatement prepareStatement(String sql) throws SQLException {
        String taintedSql = parseSql(sql);
        //logger.debug("prepareStatement(sql): "+taintedSql);
        if(sql.trim().toLowerCase().startsWith("select")) {
            return PreparedSelectStatementWrapper.wrap(delegate.prepareStatement(taintedSql),sql,taintedSql);

        }
        return PreparedStatementWrapper.wrap(delegate.prepareStatement(taintedSql),sql,taintedSql);
    }

    @Override
    public PreparedStatement prepareStatement(String sql, int resultSetType, int resultSetConcurrency) throws SQLException {
        String taintedSql = parseSql(sql);
        //logger.debug("prepareStatement(sql,resultSetType,resultSetConcurrency): "+taintedSql);
        if(sql.trim().toLowerCase().startsWith("select")) {
            return PreparedSelectStatementWrapper.wrap(delegate.prepareStatement(taintedSql, resultSetType, resultSetConcurrency),sql,taintedSql);

        }
        return PreparedStatementWrapper.wrap(delegate.prepareStatement(taintedSql, resultSetType, resultSetConcurrency),sql,taintedSql);
    }

    @Override
    public PreparedStatement prepareStatement(String sql, int resultSetType, int resultSetConcurrency, int resultSetHoldability) throws SQLException {
        String taintedSql = parseSql(sql);
        //logger.debug("prepareStatement(sql,resultSetType,resultSetConcurrency,resultSetHoldability): "+taintedSql);
        if(sql.trim().toLowerCase().startsWith("select")) {
            return PreparedSelectStatementWrapper.wrap(delegate.prepareStatement(taintedSql, resultSetType, resultSetConcurrency, resultSetHoldability),sql,taintedSql);

        }
        return PreparedStatementWrapper.wrap(delegate.prepareStatement(taintedSql, resultSetType, resultSetConcurrency, resultSetHoldability),sql,taintedSql);
    }

    @Override
    public PreparedStatement prepareStatement(String sql, int autoGeneratedKeys) throws SQLException {
        String taintedSql = parseSql(sql);
        //logger.debug("prepareStatement(sql,autoGeneratedKeys): "+taintedSql);
        if(sql.trim().toLowerCase().startsWith("select")) {
            return PreparedSelectStatementWrapper.wrap(delegate.prepareStatement(taintedSql, autoGeneratedKeys),sql,taintedSql);

        }
        return PreparedStatementWrapper.wrap(delegate.prepareStatement(taintedSql, autoGeneratedKeys),sql,taintedSql);
    }

    @Override
    public PreparedStatement prepareStatement(String sql, int[] columnIndexes) throws SQLException {
        String taintedSql = parseSql(sql);
        //logger.debug("prepareStatement(sql,columnIndexes[]): "+taintedSql);
        //TODO: fix indexes
        if(sql.trim().toLowerCase().startsWith("select")) {
            return PreparedSelectStatementWrapper.wrap(delegate.prepareStatement(taintedSql, columnIndexes),sql,taintedSql);

        }
        return PreparedStatementWrapper.wrap(delegate.prepareStatement(taintedSql, columnIndexes),sql,taintedSql);
    }

    @Override
    public PreparedStatement prepareStatement(String sql, String[] columnNames) throws SQLException {

        String taintedSql = parseSql(sql);
        //logger.debug("prepareStatement(sql,columnNames[]): "+taintedSql);
        if(sql.trim().toLowerCase().startsWith("select")) {
            return PreparedSelectStatementWrapper.wrap(delegate.prepareStatement(taintedSql, columnNames),sql,taintedSql);

        }
        return PreparedStatementWrapper.wrap(delegate.prepareStatement(taintedSql, columnNames),sql,taintedSql);
    }

    @Override
    public CallableStatement prepareCall(String sql, int resultSetType, int resultSetConcurrency, int resultSetHoldability) throws SQLException {
        sql = parseSql(sql);
        //logger.debug("prepareCall(sql,resultSetType,resultSetConcurrency,resultSetHoldability): "+sql);
        return CallableStatementWrapper.wrap(delegate.prepareCall(sql, resultSetType, resultSetConcurrency, resultSetHoldability));
    }

    @Override
    public CallableStatement prepareCall(String sql) throws SQLException {
        sql=parseSql(sql);
        //logger.debug("prepareCall(sql): "+sql);
        return CallableStatementWrapper.wrap(delegate.prepareCall(sql));
    }

    @Override
    public CallableStatement prepareCall(String sql, int resultSetType, int resultSetConcurrency) throws SQLException {
        sql = parseSql(sql);
        //logger.debug("prepareCall(sql,resultSetType,resultSetConcurrency): "+sql);
        return CallableStatementWrapper.wrap(delegate.prepareCall(sql, resultSetType, resultSetConcurrency));
    }

    @Override
    public void commit() throws SQLException {
        SQLException e = null;
        long start = System.nanoTime();
        try {
            delegate.commit();
        } catch (SQLException sqle) {
            e = sqle;
            throw e;
        }
    }

    @Override
    public void rollback() throws SQLException {
        SQLException e = null;
        long start = System.nanoTime();
        try {
            delegate.rollback();
        } catch (SQLException sqle) {
            e = sqle;
            throw e;
        }
    }

    @Override
    public void rollback(Savepoint savepoint) throws SQLException {
        SQLException e = null;
        long start = System.nanoTime();
        try {
            delegate.rollback(savepoint);
        } catch (SQLException sqle) {
            e = sqle;
            throw e;
        }
    }

    @Override
    public String nativeSQL(String sql) throws SQLException {
        return delegate.nativeSQL(sql);
    }

    @Override
    public void setAutoCommit(boolean autoCommit) throws SQLException {
        SQLException e = null;
        boolean oldAutoCommit = delegate.getAutoCommit();
        try {
            delegate.setAutoCommit(autoCommit);
        } catch (SQLException sqle){
            e = sqle;
            throw e;
        }
    }

    @Override
    public boolean getAutoCommit() throws SQLException {
        return delegate.getAutoCommit();
    }

    @Override
    public void close() throws SQLException {
        SQLException e = null;
        long start = System.nanoTime();
        try {
            delegate.close();
        } catch (SQLException sqle) {
            e = sqle;
            throw e;
        }
    }

    @Override
    public boolean isClosed() throws SQLException {
        return delegate.isClosed();
    }

    @Override
    public DatabaseMetaData getMetaData() throws SQLException {
        return delegate.getMetaData();
    }

    @Override
    public void setReadOnly(boolean readOnly) throws SQLException {
        delegate.setReadOnly(readOnly);
    }

    @Override
    public boolean isReadOnly() throws SQLException {
        return delegate.isReadOnly();
    }

    @Override
    public void setCatalog(String catalog) throws SQLException {
        delegate.setCatalog(catalog);
    }

    @Override
    public String getCatalog() throws SQLException {
        return delegate.getCatalog();
    }

    @Override
    public void setTransactionIsolation(int level) throws SQLException {
        delegate.setTransactionIsolation(level);
    }

    @Override
    public int getTransactionIsolation() throws SQLException {
        return delegate.getTransactionIsolation();
    }

    @Override
    public SQLWarning getWarnings() throws SQLException {
        return delegate.getWarnings();
    }

    @Override
    public void clearWarnings() throws SQLException {
        delegate.clearWarnings();
    }

    @Override
    public Map<String, Class<?>> getTypeMap() throws SQLException {
        return delegate.getTypeMap();
    }

    @Override
    public void setTypeMap(Map<String, Class<?>> map) throws SQLException {
        delegate.setTypeMap(map);
    }

    @Override
    public void setHoldability(int holdability) throws SQLException {
        delegate.setHoldability(holdability);
    }

    @Override
    public int getHoldability() throws SQLException {
        return delegate.getHoldability();
    }

    @Override
    public Savepoint setSavepoint() throws SQLException {
        return delegate.setSavepoint();
    }

    @Override
    public Savepoint setSavepoint(String name) throws SQLException {
        return delegate.setSavepoint(name);
    }

    @Override
    public void releaseSavepoint(Savepoint savepoint) throws SQLException {
        delegate.releaseSavepoint(savepoint);
    }

    @Override
    public Clob createClob() throws SQLException {
        return delegate.createClob();
    }

    @Override
    public Blob createBlob() throws SQLException {
        return delegate.createBlob();
    }

    @Override
    public NClob createNClob() throws SQLException {
        return delegate.createNClob();
    }

    @Override
    public SQLXML createSQLXML() throws SQLException {
        return delegate.createSQLXML();
    }

    @Override
    public boolean isValid(int timeout) throws SQLException {
        return delegate.isValid(timeout);
    }

    @Override
    public void setClientInfo(String name, String value) throws SQLClientInfoException {
        delegate.setClientInfo(name, value);
    }

    @Override
    public void setClientInfo(Properties properties) throws SQLClientInfoException {
        delegate.setClientInfo(properties);
    }

    @Override
    public String getClientInfo(String name) throws SQLException {
        return delegate.getClientInfo(name);
    }

    @Override
    public Properties getClientInfo() throws SQLException {
        return delegate.getClientInfo();
    }

    @Override
    public Array createArrayOf(String typeName, Object[] elements) throws SQLException {
        return delegate.createArrayOf(typeName, elements);
    }

    @Override
    public Struct createStruct(String typeName, Object[] attributes) throws SQLException {
        return delegate.createStruct(typeName, attributes);
    }

    @Override
    public void setSchema(String schema) throws SQLException {
        delegate.setSchema(schema);
    }

    @Override
    public String getSchema() throws SQLException {
        return delegate.getSchema();
    }

    @Override
    public void abort(Executor executor) throws SQLException {
        delegate.abort(executor);
    }

    public void setNetworkTimeout(Executor executor, int milliseconds) throws SQLException {
        delegate.setNetworkTimeout(executor, milliseconds);
    }

    @Override
    public int getNetworkTimeout() throws SQLException {
        return delegate.getNetworkTimeout();
    }

    public String parseSql(String sql){
        List<Taint> taints = new ArrayList<>();
        StatementTainter tainter = new StatementTainter(taints);
        //TODO: cleaner implementation of stmts
        Statements stmts=null;
        try {
            stmts = CCJSqlParserUtil.parseStatements(sql);
            stmts.accept(tainter);
        } catch (JSQLParserException jsqlParserException) {
            jsqlParserException.printStackTrace();
        }
        System.out.println((stmts.toString().trim()));
        return stmts.toString().trim();
    }

}

